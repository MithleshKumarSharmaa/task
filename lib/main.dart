import 'package:flutter/material.dart';
import 'package:task/google_login/login.dart';

void main() => runApp(const MyApp());

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Google login',
      home: LoginScreen(),
    );
  }
}


/*

Hello Sir, This code working and firebase connection is working good 
but few plugin is not supported by LAPTOP becauce my laptop is old. 
but code working.
thnak you.

*/ 
